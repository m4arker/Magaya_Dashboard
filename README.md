Good baseline for a Dashboard.

Handles CSV's exported from Magaya better suited to handle large bulk i.e (From March 14 - June 2nd).
Has two staitic pages, Airlines and Index, airlines handle all carrier information index does the rest.
Things to make note of in the ETL when adding a new CSV either copy the existing name that is being used in the line of code or change the name in the line of code either works I prefer chaning the file name.
